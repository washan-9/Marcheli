// ViewModel: citas: próxima cita, reprogramar, cancelar, unirse a videollamada
// Consume el orquestador (apps/api) vía models/api.ts + React Query. TODO: implementar.
export function useAppointments() {
  return {};
}
